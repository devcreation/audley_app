workflows:

  # ─── Android Debug APK ───
  android-debug:
    name: Android Debug
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: Set up Flutter project scaffold
        script: |
          flutter create --org com.distantfrontiers --project-name audley_achievers /tmp/scaffold --platforms android,ios
          cp -r "$CM_BUILD_DIR/lib" /tmp/my_lib
          cp "$CM_BUILD_DIR/pubspec.yaml" /tmp/my_pubspec.yaml
          if [ -d "$CM_BUILD_DIR/assets" ]; then cp -r "$CM_BUILD_DIR/assets" /tmp/my_assets; fi
          rm -rf "$CM_BUILD_DIR/android" && cp -r /tmp/scaffold/android "$CM_BUILD_DIR/android"
          rm -rf "$CM_BUILD_DIR/ios" && cp -r /tmp/scaffold/ios "$CM_BUILD_DIR/ios"
          for f in .metadata; do [ -f "/tmp/scaffold/$f" ] && cp "/tmp/scaffold/$f" "$CM_BUILD_DIR/$f"; done
          rm -rf "$CM_BUILD_DIR/lib" && cp -r /tmp/my_lib "$CM_BUILD_DIR/lib"
          cp /tmp/my_pubspec.yaml "$CM_BUILD_DIR/pubspec.yaml"
          if [ -d "/tmp/my_assets" ]; then rm -rf "$CM_BUILD_DIR/assets"; cp -r /tmp/my_assets "$CM_BUILD_DIR/assets"; fi

      - name: Install dependencies
        script: flutter pub get

      - name: Set app display name
        script: dart run rename setAppName --targets android,ios --value "Audley's Top Performers Incentive"

      - name: Generate app icon
        script: dart run flutter_launcher_icons

      - name: Build debug APK
        script: flutter build apk --debug

    artifacts:
      - build/**/outputs/**/*.apk


  # ─── Android Play Store (Signed AAB) ───
  android-release:
    name: Android Play Store
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      groups:
        - android_signing
    scripts:
      - name: Set up Flutter project scaffold
        script: |
          flutter create --org com.distantfrontiers --project-name audley_achievers /tmp/scaffold --platforms android,ios
          cp -r "$CM_BUILD_DIR/lib" /tmp/my_lib
          cp "$CM_BUILD_DIR/pubspec.yaml" /tmp/my_pubspec.yaml
          if [ -d "$CM_BUILD_DIR/assets" ]; then cp -r "$CM_BUILD_DIR/assets" /tmp/my_assets; fi
          rm -rf "$CM_BUILD_DIR/android" && cp -r /tmp/scaffold/android "$CM_BUILD_DIR/android"
          rm -rf "$CM_BUILD_DIR/ios" && cp -r /tmp/scaffold/ios "$CM_BUILD_DIR/ios"
          for f in .metadata; do [ -f "/tmp/scaffold/$f" ] && cp "/tmp/scaffold/$f" "$CM_BUILD_DIR/$f"; done
          rm -rf "$CM_BUILD_DIR/lib" && cp -r /tmp/my_lib "$CM_BUILD_DIR/lib"
          cp /tmp/my_pubspec.yaml "$CM_BUILD_DIR/pubspec.yaml"
          if [ -d "/tmp/my_assets" ]; then rm -rf "$CM_BUILD_DIR/assets"; cp -r /tmp/my_assets "$CM_BUILD_DIR/assets"; fi

      - name: Install dependencies
        script: flutter pub get

      - name: Set app display name
        script: dart run rename setAppName --targets android,ios --value "Audley's Top Performers Incentive"

      - name: Generate app icon
        script: dart run flutter_launcher_icons

      - name: Generate keystore
        script: |
          if [ -z "$CM_KEYSTORE" ]; then
            keytool -genkey -v \
              -keystore "$CM_BUILD_DIR/android/app/release-keystore.jks" \
              -alias "$CM_KEY_ALIAS" \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass "$CM_KEYSTORE_PASSWORD" \
              -keypass "$CM_KEY_PASSWORD" \
              -dname "CN=Distant Frontiers, OU=Dev, O=Distant Frontiers, L=Delhi, ST=Delhi, C=IN"
          else
            echo "$CM_KEYSTORE" | base64 --decode > "$CM_BUILD_DIR/android/app/release-keystore.jks"
          fi

      - name: Configure signing
        script: |
          cat > "$CM_BUILD_DIR/android/key.properties" << EOF
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=release-keystore.jks
          EOF

          # Patch build.gradle to use signing config
          GRADLE="$CM_BUILD_DIR/android/app/build.gradle"
          if [ -f "$GRADLE" ]; then
            sed -i 's/buildTypes {/signingConfigs { release { def props = new Properties(); def f = file("..\/key.properties"); if(f.exists()){props.load(new FileInputStream(f))}; storeFile file(props["storeFile"]); storePassword props["storePassword"]; keyAlias props["keyAlias"]; keyPassword props["keyPassword"] } }\n    buildTypes {/' "$GRADLE"
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/' "$GRADLE"
          fi
          # Handle build.gradle.kts
          GRADLE_KTS="$CM_BUILD_DIR/android/app/build.gradle.kts"
          if [ -f "$GRADLE_KTS" ] && ! [ -f "$GRADLE" ]; then
            python3 -c "
          import re
          with open('$GRADLE_KTS','r') as f: c=f.read()
          signing_block = '''
      signingConfigs {
          create("release") {
              val props = java.util.Properties()
              val f = file("../key.properties")
              if (f.exists()) { props.load(java.io.FileInputStream(f)) }
              storeFile = file(props.getProperty("storeFile", "release-keystore.jks"))
              storePassword = props.getProperty("storePassword", "")
              keyAlias = props.getProperty("keyAlias", "")
              keyPassword = props.getProperty("keyPassword", "")
          }
      }
          '''
          c = c.replace('buildTypes {', signing_block + '\n    buildTypes {')
          c = c.replace('signingConfig = signingConfigs.getByName("debug")', 'signingConfig = signingConfigs.getByName("release")')
          with open('$GRADLE_KTS','w') as f: f.write(c)
          print('build.gradle.kts patched')
          "
          fi

      - name: Build signed AAB
        script: flutter build appbundle --release

    artifacts:
      - build/**/outputs/**/*.aab
      - build/**/outputs/**/mapping.txt


  # ─── iOS IPA Build (Testing) ───
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: ad_hoc
        bundle_identifier: com.distantfrontiers.audleyAchievers
    scripts:
      - name: Set up Flutter project scaffold
        script: |
          echo "==> Creating Flutter project scaffold..."
          flutter create --org com.distantfrontiers --project-name audley_achievers /tmp/scaffold --platforms android,ios

          echo "==> Backing up source code..."
          cp -r "$CM_BUILD_DIR/lib" /tmp/my_lib
          cp "$CM_BUILD_DIR/pubspec.yaml" /tmp/my_pubspec.yaml

          if [ -d "$CM_BUILD_DIR/assets" ]; then
            cp -r "$CM_BUILD_DIR/assets" /tmp/my_assets
          fi

          echo "==> Copying scaffold platform directories..."
          rm -rf "$CM_BUILD_DIR/android"
          cp -r /tmp/scaffold/android "$CM_BUILD_DIR/android"

          rm -rf "$CM_BUILD_DIR/ios"
          cp -r /tmp/scaffold/ios "$CM_BUILD_DIR/ios"

          for f in .metadata; do
            if [ -f "/tmp/scaffold/$f" ]; then
              cp "/tmp/scaffold/$f" "$CM_BUILD_DIR/$f"
            fi
          done

          echo "==> Restoring source code on top of scaffold..."
          rm -rf "$CM_BUILD_DIR/lib"
          cp -r /tmp/my_lib "$CM_BUILD_DIR/lib"
          cp /tmp/my_pubspec.yaml "$CM_BUILD_DIR/pubspec.yaml"

          if [ -d "/tmp/my_assets" ]; then
            rm -rf "$CM_BUILD_DIR/assets"
            cp -r /tmp/my_assets "$CM_BUILD_DIR/assets"
          fi

      - name: Install dependencies
        script: |
          flutter pub get

      - name: Set app display name
        script: |
          dart run rename setAppName --targets android,ios --value "Audley's Top Performers Incentive"

      - name: Generate app icon
        script: |
          dart run flutter_launcher_icons

      - name: Install CocoaPods
        script: |
          cd ios && pod install

      - name: Set up iOS code signing
        script: |
          xcode-project use-profiles

      - name: Build IPA
        script: |
          flutter build ipa --release --export-options-plist=/Users/builder/export_options.plist

    artifacts:
      - build/ios/ipa/*.ipa


  # ─── Web Build (Test on any iPhone without Apple account) ───
  web-workflow:
    name: Web Build
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: 3.22.3
    scripts:
      - name: Set up Flutter project scaffold
        script: |
          flutter create --org com.distantfrontiers --project-name audley_achievers /tmp/scaffold --platforms web

          cp -r "$CM_BUILD_DIR/lib" /tmp/my_lib
          cp "$CM_BUILD_DIR/pubspec.yaml" /tmp/my_pubspec.yaml
          if [ -d "$CM_BUILD_DIR/assets" ]; then cp -r "$CM_BUILD_DIR/assets" /tmp/my_assets; fi

          cp -r /tmp/scaffold/web "$CM_BUILD_DIR/web"

          rm -rf "$CM_BUILD_DIR/lib"
          cp -r /tmp/my_lib "$CM_BUILD_DIR/lib"
          cp /tmp/my_pubspec.yaml "$CM_BUILD_DIR/pubspec.yaml"
          if [ -d "/tmp/my_assets" ]; then rm -rf "$CM_BUILD_DIR/assets"; cp -r /tmp/my_assets "$CM_BUILD_DIR/assets"; fi

      - name: Install dependencies
        script: flutter pub get

      - name: Build web
        script: flutter build web --release --base-href /audleyachievers/app/ --web-renderer html

      - name: Prepare output
        script: |
          cd build/web

          # Favicon
          if [ -f "assets/assets/icon.png" ]; then
            cp assets/assets/icon.png favicon.png
          fi

          # IIS web.config — overrides parent .json block + serves all static files
          python3 -c "
          content = '''<?xml version=\"1.0\" encoding=\"UTF-8\"?>
          <configuration>
            <system.webServer>
              <defaultDocument>
                <files><clear /><add value=\"index.html\" /></files>
              </defaultDocument>
              <security>
                <requestFiltering>
                  <fileExtensions allowUnlisted=\"true\">
                    <clear />
                  </fileExtensions>
                </requestFiltering>
              </security>
              <handlers>
                <clear />
                <add name=\"StaticFile\" path=\"*\" verb=\"*\" modules=\"StaticFileModule,DefaultDocumentModule,DirectoryListingModule\" resourceType=\"Either\" requireAccess=\"Read\" />
              </handlers>
              <staticContent>
                <remove fileExtension=\".json\" />
                <mimeMap fileExtension=\".json\" mimeType=\"application/json\" />
                <remove fileExtension=\".js\" />
                <mimeMap fileExtension=\".js\" mimeType=\"application/javascript\" />
                <remove fileExtension=\".otf\" />
                <mimeMap fileExtension=\".otf\" mimeType=\"font/otf\" />
                <remove fileExtension=\".ttf\" />
                <mimeMap fileExtension=\".ttf\" mimeType=\"font/ttf\" />
                <remove fileExtension=\".woff2\" />
                <mimeMap fileExtension=\".woff2\" mimeType=\"font/woff2\" />
                <remove fileExtension=\".wasm\" />
                <mimeMap fileExtension=\".wasm\" mimeType=\"application/wasm\" />
              </staticContent>
            </system.webServer>
          </configuration>'''
          with open('web.config', 'w') as f:
              f.write(content)
          print('web.config created')
          "

          echo "Output ready"

      - name: Package web build
        script: |
          cd build/web && zip -r ../../web-build.zip .

    artifacts:
      - web-build.zip
